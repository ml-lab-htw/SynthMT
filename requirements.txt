# =============================================================================
# SynthMT - Core Dependencies
# =============================================================================
# Python >=3.11 required

# Core scientific computing
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
scikit-image>=0.20.0
pandas>=2.0.0

# Image processing
opencv-python>=4.7.0
Pillow>=9.4.0
imageio>=2.25.0
tifffile>=2023.2.0
albumentations>=1.3.0

# Deep learning core
torch>=2.0.0
torchvision>=0.15.0

# Transformers (pre-release for SAM3 support)
transformers>=4.47.0

# HuggingFace ecosystem
datasets>=2.14.0
huggingface-hub>=0.17.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Optimization
optuna>=3.3.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
python-dotenv>=1.0.0
cachetools>=5.3.0

# =============================================================================
# Model-Specific Dependencies (Optional - install as needed)
# =============================================================================

# Cellpose & CellposeSAM
cellpose>=3.0.0

# CellSAM (requires DEEPCELL_ACCESS_TOKEN in .env)
# cellSAM  # Install separately: pip install git+https://github.com/mario-koddenbrock/cellSAM.git

# StarDist (requires tensorflow)
stardist==0.9.1

# TARDIS
tardis-em==0.3.10  # Install separately: pip install tardis-em==0.3.10

# LPIPS (for perceptual similarity)
lpips>=0.1.4

# =============================================================================
# Development & Testing
# =============================================================================
pytest>=7.0.0
pytest-cov>=4.0.0
tox>=4.0.0

# =============================================================================
# Notes:
# =============================================================================
# - For SAM3 support, install transformers pre-release:
#     pip install -U transformers --pre
#
# - For µSAM (MicroSAM), install via conda:
#     conda install -c conda-forge micro_sam=1.6.2
#
# - For CellSAM, you need a DEEPCELL_ACCESS_TOKEN:
#     pip install git+https://github.com/mario-koddenbrock/cellSAM.git
#     Create .env file with: DEEPCELL_ACCESS_TOKEN=your_token
#
# - For TARDIS:
#     pip install tardis-em==0.3.10
